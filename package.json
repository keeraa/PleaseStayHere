{
  "name": "pleasestayhere",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local rental search collector MVP: Facebook groups -> SQLite -> normalized listings",
  "scripts": {
    "login": "node src/facebook/login.js",
    "collect": "node src/cli.js collect",
    "import:fixture": "node src/cli.js import-fixture tests/fixture-posts.json",
    "stats": "node src/cli.js stats",
    "test": "node --test tests/*.test.js",
    "dev": "node src/server.js"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
